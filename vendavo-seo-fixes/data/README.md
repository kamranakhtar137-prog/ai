# Static schema files (reference only)

These files are **snapshots** from a point in time. Do not use them for production.

Use the **dynamic plugin code** instead:

- `includes/class-platform-schema.php`
- `includes/class-schema-parser.php`
- `includes/class-organization-schema.php`

The plugin generates JSON-LD automatically from live page content and Elementor data.
